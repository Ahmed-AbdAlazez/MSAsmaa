# وثيقة هيكل المشروع | Project Structure Documentation

موقع "المرسال" هو نموذج أولي ثابت (Static Prototype) لواجهة وتجربة المستخدم الخاصة بمنصة تعليمية لمادة الأحياء لطلاب الثانوية العامة في مصر.

---

## 1. شجرة الملفات والجلد | Folder & File Structure Tree

```text
c:/Users/moham/OneDrive/Desktop/MSAsmaa/
├── index.html                   # الصفحة الرئيسية مع دليل المسارات (Landing Page)
├── dashboard-student.html       # لوحة تحكم الطالب (Student Dashboard)
├── lessons.html                 # دليل المسارات والدروس العام (Course Directory)
├── course-biology.html          # منهج الأحياء - الصف الثالث الثانوي (Biology Syllabus)
├── course-integrated.html       # منهج العلوم المتكاملة - الصف الأول الثانوي (Integrated Science)
├── lesson-view.html             # صفحة عرض الدرس المفرد والفيديو (Single Lesson View)
├── assignments.html             # قائمة الواجبات والاختبارات (Assignments List)
├── assignment-view.html         # صفحة تفاصيل وحل الواجب (Single Assignment Form)
├── dashboard-teacher.html       # لوحة تحكم المعلم (Teacher Dashboard)
├── about.html                   # صفحة من نحن والتواصل (About & Contact)
├── css/
│   └── style.css                # ورقة الأنماط الرئيسية والمكونات (Shared stylesheet)
├── js/
│   └── main.js                  # التفاعلات الحركية البسيطة (Responsive layout menu, simulated redirects)
└── PROJECT_STRUCTURE.md         # هذه الوثيقة التوضيحية (Documentation)
```

---

## 2. الصفحات والغرض منها | Pages and Purposes

1. **الصفحة الرئيسية (`index.html`)**: عرض تسويقي وتعريفي بالمنصة والميزات والمسارات التعليمية المتاحة كبطاقات منفصلة.
2. **لوحة تحكم الطالب (`dashboard-student.html`)**: واجهة الطالب لمتابعة نسبة تقدمه في الأبواب الدراسية، والواجبات المتبقية والدرجات المحصلة.
3. **فهرس المناهج (`lessons.html`)**: صفحة وسيطة تعمل كدليل للمسارات وتوجه الطالب للمنهج المناسب لسنته الدراسية.
4. **منهج الأحياء (`course-biology.html`)**: الصفحة الرسمية لمنهج الصف الثالث الثانوي مع ترتيب الأكورديون للوحدات والدروس.
5. **منهج العلوم المتكاملة (`course-integrated.html`)**: الصفحة الرسمية لمنهج الصف الأول الثانوي مع ترتيب الأكورديون للوحدات والدروس.
6. **عرض الدرس (`lesson-view.html`)**: صفحة تفاعلية تظهر مشغل فيديو تعليمي، وشرح نصي للمفاهيم، ومصطلحات علمية يتم تعبئتها ديناميكياً بناءً على اسم الدرس الممرر في الرابط.
7. **فهرس الواجبات (`assignments.html`)**: قائمة بالمهام والواجبات المنزلية المطلوبة من الطالب مع إظهار حالة كل واجب (مكتمل / معلق / متأخر).
8. **حل الواجب (`assignment-view.html`)**: عرض أسئلة الاختيار من متعدد للمنهج وتوفير مساحة لرفع ملف الإجابة أو كتابة الملاحظات.
9. **لوحة تحكم المعلم (`dashboard-teacher.html`)**: إحصائيات عامة عن الطلاب ومستوى تسليم الواجبات مع جدول تفصيلي لدرجات وحضور الطلاب.
10. **عن المنصة والتواصل (`about.html`)**: نبذة عن مدرسة المادة الأستاذة أسماء مرسال، ورؤية المنصة، ونموذج إرسال رسائل استفسار.
*(ملاحظة: تم إلغاء صفحة تسجيل الدخول المستقلة `login.html` واستبدالها بنوافذ ترحيب ونوافذ تسجيل دخول منبثقة تفاعلية بالكامل عبر أيقونة حساب المستخدم في شريط التنقل العلوي)*

---

## 3. المكونات المشتركة وقابلة لإعادة الاستخدام | Reusable CSS Components

جميع المكونات مصممة بأسلوب الأنماط المشتركة في ملف `css/style.css` ومطبقة عبر جميع الصفحات لتوحيد الهوية البصرية:

* **شريط التنقل (`.navbar`)**: يظهر في أعلى كافة الصفحات. يحتوي على شعار المنصة، روابط التنقل السريع، وزر تسجيل الدخول / الخروج. يدعم التجاوب مع الجوال عبر قائمة برجر منزلقة.
* **التذييل (`.footer`)**: يظهر أسفل كل الصفحات، يحتوي على معلومات الاتصال، روابط المنهج، وحقوق النشر.
* **البطاقات (`.card`)**: تستخدم لعرض الدروس، إحصائيات لوحات التحكم، والمميزات. تدعم تأثيرات التحويم الظلية البسيطة لمظهر عصري.
* **الأزرار (`.btn`)**: فئات أزرار مرنة تشمل الزر الرئيسي (`.btn-primary`) بلون أخضر داكن، والزر الثانوي (`.btn-secondary`)، وزر الخطر (`.btn-danger`).
* **شارات الحالة (`.badge`)**: تستخدم لعرض حالة الواجبات أو درجات الطلاب وتأتي بثلاثة ألوان: أخضر للمكتمل، برتقالي للمعلق، وأحمر للمتأخر.
* **جداول البيانات (`.table`)**: تستخدم في لوحة المعلم لعرض درجات الطلاب والنسب المئوية بشكل نظيف ومنسق.
* **حقول النماذج (`.form-group`)**: نمط موحد لحقول النص والاختيار والقوائم المنسدلة في صفحات تسجيل الدخول، التواصل، وحل الواجب.

---

## 4. نظام الألوان والخطوط | Color Palette & Typography

تم اختيار لوحة ألوان تعبر عن الطبيعة الحيوية للأحياء مع لمسة أكاديمية جادة:

### الألوان المستخدمة (Hex Codes):
* **الأخضر الغابي (الأساسي) | Primary Green**: `#0F4C3A` (يستخدم في الهوية البصرية والترويسة والأزرار الهامة).
* **الأخضر الزمردي (التأكيدي) | Emerald Accent**: `#10B981` (يستخدم للمؤشرات، نسب التقدم، والشارات الإيجابية).
* **الرمادي الغامق (الخطوط) | Slate Neutral**: `#0F172A` (يستخدم للنصوص والعناوين الرئيسية).
* **خلفية الموقع | Light Background**: `#F8FAFC` (خلفية صفحات المنصة).
* **الخلفية السطحية | Surface White**: `#FFFFFF` (خلفية البطاقات والجداول والواجبات).
* **اللون الذهبي/البرتقالي | Gold Accent**: `#D97706` (للتحذيرات والواجبات المعلقة والتنبيهات).
* **الحدود الفاصلة | Border Light**: `#E2E8F0` (للخطوط والحدود الفاصلة الرقيقة).

### الخطوط (Typography):
* تم استخدام خط **Tajawal** العربي من Google Fonts لجماله ووضوح حروفه العلمية.
* تم استخدام خط **Inter** اللاتيني للأرقام والرموز والمصطلحات الإنجليزية لضمان مظهر احترافي ومقروئية عالية.

---

## 5. اتفاقيات البيانات الصورية والنائبة | Placeholder & Dummy Data Conventions

تم استخدام بيانات ثابتة مطابقة للمنهج المصري للثانوية العامة للتأكد من ملاءمتها للسياق العلمي:
* **أسماء الطلاب الصورية**: أحمد محمد الشافعي، عمر مصطفى كمال، سارة عبد الرحمن، يوسف طارق علام.
* **المسارات التعليمية وهيكل المناهج المعتمدة**:
  * **المسار الأول: الأحياء - الصف الثالث الثانوي (علمي علوم)**
    * الفصل الدراسي الأول: الدعامة والحركة.
    * الفصل الدراسي الثاني: التكاثر، الوراثة، النظام البيئي.
  * **المسار الثاني: العلوم المتكاملة - الصف الأول الثانوي**
    * الفصل الدراسي الأول: النظام البيئي المائي، الغلاف الجوي.
    * الفصل الدراسي الثاني: الطاقة في النظم البيئية، مصادر الطاقة، إعادة التدوير وفصل المواد.
* **التقييمات والدرجات**: درجات ثابتة مثل `8/10` و `10/10` ونسب مئوية مثل `85%` و `70%`.
* **الفيديو**: مساحة مشغل الفيديو تأتي بلون داكن `#1E293B` مع أيقونة تشغيل مركزي ونص توضيحي، دون استدعاء يوتيوب أو سيرفر خارجي لتلافي بطء التحميل وتسهيل العرض المحلي.

---

## 6. الخطوات المستقبلية للتشغيل الفعلي | Next Steps for Production

لتحويل هذا النموذج الأولي البصري إلى منصة تعليمية حقيقية متكاملة، يجب إضافة التالي:
1. **نظام إدارة المستخدمين (Authentication & RBAC)**:
   * ربط الواجهة بقاعدة بيانات وإضافة نظام تسجيل دخول حقيقي (JWT أو Firebase Auth) وتحديد الصلاحيات بشكل صارم (طالب، مدرس، ولي أمر).
2. **قاعدة بيانات ديناميكية (Database)**:
   * استخدام PostgreSQL أو MongoDB لتخزين بيانات الطلاب، الدروس، الفيديوهات، والواجبات والدرجات بدلاً من البيانات الثابتة.
3. **مشغل فيديو سحابي محمي (Secure Video Streaming)**:
   * ربط مشغل الفيديو بخدمات حماية مثل Vimeo OTT أو AWS CloudFront لضمان عدم تحميل الفيديوهات أو تسريبها وحفظ حقوق المدرس.
4. **نظام تصحيح الواجبات والاختبارات التفاعلي**:
   * إضافة لوحة تفاعلية تسمح للطلاب بحل اختبارات تصحح تلقائياً فورياً ونظام لرفع ملفات الواجب بصيغة PDF وتخزينها في AWS S3 ليصححها المعلم يدوياً.
5. **لوحة تحكم معززة للمعلم**:
   * تمكين المدرس من إضافة دروس جديدة وتعديل الواجبات ورؤية تقارير تحليلية متقدمة لأداء الطلاب مدعومة بالرسوم البيانية التفاعلية.
